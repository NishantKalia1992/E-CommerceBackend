# 1. Connect to localhost - dev


server.port=8004
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
management.zipkin.tracing.endpoint=http://localhost:9411/api/v2/spans

# ==========================================
# Google OAuth2 Login Configuration
# ==========================================
spring.security.oauth2.client.registration.google.client-id=183931541747-rr6rtncut747a9h38vv89bmgqf7o2oam.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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

# Optional but recommended settings
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.provider=google

# 2. circuit breaker config
#resilience4j.circuitbreaker.instances.customerServiceBreaker.sliding-window-size=5
#resilience4j.circuitbreaker.instances.customerServiceBreaker.failure-rate-threshold=50
#resilience4j.circuitbreaker.instances.customerServiceBreaker.wait-duration-in-open-state=10s

#eureka.instance.prefer-ip-address=true

# 1. Tell Micrometer to record 100% of all requests (1.0 = 100%)
# (In production, companies usually set this to 0.1 so they only record 10% to save memory)
#management.tracing.sampling.probability=1.0

# 2. Tell the services where the Zipkin Docker container lives